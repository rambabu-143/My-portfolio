/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/My-portfolio",
    assetPrefix: "/My-portfolio",
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
